# `@cfwest/ranking` - RankingModule

> This package is used to query CrossFire West Player & Clan Ranking Stats

## Building

To build the project run `npm run build`  

## Testing

To execute the tests run `npm run test`